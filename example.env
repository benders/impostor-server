# Public hostname for the game server (must match your DNS record)
PUBLIC_HOST=impostor.example.com

# Cloudflare API token for automatic TLS certificate issuance via DNS challenge
# See README.md for required token permissions
CF_API_TOKEN=your_cloudflare_token_here
